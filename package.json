{
	"name": "www-afrotc-org",
	"version": "2.1.1",
	"private": true,
	"scripts": {
		"dev": "next dev --turbopack",
		"build": "npx wrangler types --env-interface CloudflareEnv ./env.d.ts && next build",
		"build:prod": "npx @cloudflare/next-on-pages && npm run assets:ignore && npx wrangler build --env prod",
		"build:dev": "npx @cloudflare/next-on-pages && npm run assets:ignore && npx wrangler build --env dev",
		"start": "next start",
		"lint": "eslint .",
		"test": "vitest run",
		"preview": "npm run build:prod && npx wrangler dev -e prod",
		"deploy": "npm run build:prod && npx wrangler deploy -e prod",
		"upload": "npm run build:prod && npx wrangler upload -e prod",
		"preview:dev": "npm run build:dev && npx wrangler dev --env dev",
		"deploy:dev": "npm run build:dev && npx wrangler deploy -e dev",
		"upload:dev": "npm run build:dev && npx wrangler upload -e dev",
		"cf-typegen": "npx wrangler types --env-interface CloudflareEnv ./env.d.ts",
		"assets:ignore": "mkdir -p .vercel/output/static && printf '_worker.js\n' > .vercel/output/static/.assetsignore",
		"upgrade": "npx npm-check-updates -u && npm install"
	},
	"dependencies": {
		"@opennextjs/cloudflare": "^1.14.10",
		"next": "16.1.4",
		"react": "^19.2.3",
		"react-dom": "^19.2.3"
	},
	"devDependencies": {
		"@eslint/eslintrc": "^3",
		"@eslint/js": "^9",
		"@next/eslint-plugin-next": "^16.1.4",
		"@tailwindcss/postcss": "^4",
		"@typescript-eslint/eslint-plugin": "^8",
		"@typescript-eslint/parser": "^8",
		"@types/node": "^25.0.9",
		"@types/react": "^19",
		"@types/react-dom": "^19",
		"eslint": "^9",
		"eslint-config-next": "^16.1.4",
		"eslint-plugin-react": "^7",
		"eslint-plugin-react-hooks": "^5",
		"globals": "^15",
		"npm-check-updates": "^19.3.1",
		"tailwindcss": "^4",
		"typescript": "^5",
		"vitest": "^4.0.17",
		"wrangler": "^4.59.2"
	}
}
